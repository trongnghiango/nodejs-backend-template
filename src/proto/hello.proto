syntax = "proto3";

package hello;

// Định nghĩa service
service Greeter {
  rpc SayHello (HelloRequest) returns (HelloResponse) {}
}

// Định nghĩa message
message HelloRequest {
  string name = 1;
}

message HelloResponse {
  string message = 1;
}